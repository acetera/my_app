<%= form_for(@users_db) do |f| %>
  <% if @users_db.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_db.errors.count, "error") %> prohibited this users_db from being saved:</h2>

      <ul>
      <% @users_db.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_area :username %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
